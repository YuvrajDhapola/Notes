Why I used localStorage
I chose localStorage to keep things simple, no backend needed. It lets users keep their notes even if they close or refresh the page. I used the key name "notes" so it's easy to understand and manage later on if I need to expand the app.

Styling approach
I kept the design clean and minimal. I used separate CSS files for each component. The layout is responsive.